반도체 프로브 마크 검사(PMI) 고도화를 위한 이미지 전처리, SAM 2 파운데이션 모델 및 강화학습(RLHF) 결합에 관한 심층 연구 보고서1. 서론: 프로브 마크 검사의 기술적 난제와 비전 파운데이션 모델의 도입현대 반도체 제조 산업에서 웨이퍼 레벨의 전기적 테스트(Wafer Sort)는 전체 공정의 수율과 품질을 결정짓는 가장 중요한 관문 중 하나이다. 이 과정에서 프로브 카드(Probe Card)에 장착된 미세한 프로브 니들(Probe Needle)은 반도체 다이(Die)의 알루미늄 또는 구리 재질의 인터커넥션 패드(Interconnection Pad) 표면에 직접 물리적으로 접촉하여 전기적 신호를 주고받는다. 이러한 접촉 과정은 필연적으로 패드 표면에 긁힘 자국, 즉 프로브 마크(Probe Mark 또는 Scrub Mark)를 남기게 된다. 프로브 마크 검사(Probe Mark Inspection, 이하 PMI)는 검사 비전 시스템을 통해 이 접촉 마크의 면적, 위치, 깊이 및 형태학적 일관성을 정밀하게 측정함으로써 프로브 니들의 정렬 상태, 인가된 접촉 압력의 적절성, 그리고 접촉 불량 여부를 종합적으로 판단하는 필수적인 품질 관리 공정이다. 프로브 마크가 패드의 지정된 영역을 벗어나거나 과도한 압력으로 인해 패드 하부의 절연층까지 파괴하는 현상이 발생할 경우, 이는 곧바로 칩의 치명적인 물리적 결함으로 직결되기 때문에 PMI 알고리즘의 세그멘테이션(Segmentation) 정확도는 반도체 제조 공정의 핵심 기술로 간주된다.최근 컴퓨터 비전 분야에서는 메타(Meta)가 공개한 Segment Anything Model 2 (SAM 2)와 같은 초대규모 비전 파운데이션 모델(Vision Foundation Model)이 등장하면서, 산업용 결함 검사 패러다임에 혁명적인 변화가 일고 있다. SAM 2는 1,100만 장 이상의 방대한 이미지와 11억 개 이상의 마스크로 구성된 SA-1B 데이터셋, 그리고 5만 개 이상의 비디오와 64만 개의 마스크렛(Masklet)이 포함된 SA-V 데이터셋을 바탕으로 사전 학습된 모델이다. 이 방대한 학습 데이터 덕분에 SAM 2는 이전에 본 적 없는 새로운 도메인의 이미지에 대해서도 점(Point), 바운딩 박스(Bounding Box), 또는 텍스트와 같은 시각적 프롬프트(Visual Prompt)만 주어지면 영샷(Zero-shot) 방식으로 객체의 마스크를 추출해내는 탁월한 일반화 능력을 보유하고 있다.그러나 이러한 SAM 2를 프로브 마크 검사와 같은 고도로 특화된 산업용 표면 결함 세그멘테이션에 별도의 미세 조정(Fine-tuning) 없이 영샷으로 직접 적용할 경우, 심각한 인식 오류와 성능 저하라는 기술적 장벽에 직면하게 된다. 그 근본적인 원인은 반도체 인터커넥션 패드 표면이 가지는 고유의 광학적, 구조적 특성에 있다. 패드 표면은 거울처럼 매끄러운 단일 색상의 평면이 아니며, 금속 박막 증착 및 평탄화 공정에서 발생하는 알루미늄 그레인(Grain) 구조, 표면 텍스처, 구조적 줄무늬(Striped regular pattern) 등 복잡한 배경 패턴을 내포하고 있다. 문제는 이러한 패드의 고유한 배경 패턴이 형성하는 엣지(Edge)나 명암 대비가 실제 불량 요인인 프로브 마크의 텍스처와 시각적으로 매우 유사하다는 점이다. SAM 2의 비전 트랜스포머(Vision Transformer, ViT) 기반 이미지 인코더는 명시적인 시맨틱 클래스(Semantic Class) 라벨을 할당하지 않는 클래스 불가지론적(Class-agnostic) 방식으로 동작하기 때문에, 강한 그래디언트(Gradient)를 발생시키는 패드의 정상적인 배경 텍스처를 독립적인 형태의 결함이나 대상 객체로 오인식하는 결과를 낳는다. 이는 결과적으로 프로브 마크 영역을 정확히 분할하지 못하고 배경의 무의미한 줄무늬나 거칠기 패턴을 마스크로 추출하는 치명적인 과분할(Over-segmentation) 현상으로 이어진다.인간 검사 작업자들은 수많은 시각적 노이즈와 복잡한 배경 패턴 속에서도 프로브 마크 특유의 형태적 맥락과 긁힘 방향, 깊이에 따른 미세한 명암 차이를 직관적으로 식별하여 대상 객체와 배경을 완벽하게 분리해낸다. 인간이 지닌 이러한 고도화된 맥락적 시각 추론 능력을 인공지능 모델에 효과적으로 이식하기 위해서는 단순히 입력 이미지를 픽셀 단위로 매핑하는 전통적인 지도 학습(Supervised Learning) 방식만으로는 한계가 명확하다. 이에 대한 대안으로, 인간의 직관적인 피드백을 모델의 의사결정 과정에 직접적으로 결합하는 휴먼 피드백 기반 강화학습(Reinforcement Learning from Human Feedback, RLHF)과 인터랙티브 세그멘테이션 에이전트(Interactive Segmentation Agent) 기술의 도입이 필수적으로 요구되고 있다. 본 연구는 프로브 카드 PMI의 정확도를 극대화하기 위해, 이미지 주파수 도메인 분석에 기반한 고도화된 전처리 기법(Preprocessing)을 설계하여 배경 패턴을 원천적으로 억제하는 방안을 모색하고, SAM 2의 영샷 추론 능력을 강화학습 알고리즘으로 통제 및 최적화하는 포괄적이고 심층적인 아키텍처 개선 모델을 제시하고자 한다.2. 프로브 패드의 광학적 노이즈 억제를 위한 주파수 도메인 전처리 파이프라인프로브 마크와 배경 텍스처가 복잡하게 혼재되어 있는 상황에서, 어떠한 전처리 과정도 없이 SAM 2에 원본 검사 이미지를 그대로 입력하는 것은 트랜스포머의 어텐션(Attention) 메커니즘이 프로브 마크가 아닌 배경의 고주파 노이즈에 집중하도록 방치하는 것과 같다. 반도체 패드 이미지에 존재하는 공간적 패턴(Spatial patterns), 즉 구조, 텍스처, 혹은 정상적인 표면 요철 등은 프로브 마크 검사와 무관한 정보이며 오히려 검사 알고리즘의 성능을 심각하게 저해하는 주된 원인이 된다. 따라서 모델 추론 이전 단계에서 프로브 마크 본연의 형상과 엣지 정보는 훼손하지 않으면서도, 배경의 반복적인 구조적 패턴이나 방향성 텍스처만을 선택적으로 필터링하여 억제하는 정교한 이미지 전처리(Digital Image Preprocessing) 과정이 절대적으로 필요하다.2.1. 가보어 필터(Gabor Filter)의 이론적 배경과 포유류 시각 피질의 수학적 모델링반도체 인터커넥션 패드 이미지에서 관찰되는 배경 패턴, 예를 들어 압연 공정이나 연마 공정으로 인해 생성되는 미세한 직선 형태의 줄무늬 패턴은 주파수 도메인(Frequency Domain) 관점에서 볼 때 특정한 공간 주파수(Spatial Frequency) 대역에 밀집되어 있으며 뚜렷한 방향성(Orientation)을 가진다. 픽셀 도메인에서 이러한 패턴을 분리하기란 매우 어렵지만, 특정 주파수와 방향의 성분을 가장 정밀하고 효과적으로 분해할 수 있는 수학적 도구가 바로 가보어 필터(Gabor Filter)이다. 가보어 필터는 선형 필터의 일종으로, 특정한 주파수와 방향을 가진 사인파(Sinusoidal wave) 신호를 2차원 가우시안 윈도우(Gaussian window)로 변조한 형태를 띤다. 흥미롭게도 이 가보어 함수의 수학적 특성은 고양이 등 포유류의 일차 시각 피질(Primary Visual Cortex, V1)에 존재하는 단순 세포(Simple cell)가 시각적 자극의 주파수와 방향에 수용적으로 반응하는 방식(Receptive field)과 놀라울 정도로 유사하게 동작한다. 이러한 생물학적 타당성으로 인해 가보어 필터는 텍스처 분석, 엣지 감지, 표면 평가, 그리고 불량 검출 알고리즘에서 지배적으로 사용되어 왔다.기본적인 2차원 가보어 커널의 수학적 모델은 공간 도메인에서 다음과 같은 방정식으로 정의된다.$$G(x, y) = \exp \left( -\frac{x'^2 + \gamma^2 y'^2}{2\sigma^2} \right) \cos \left( 2\pi \frac{x'}{\lambda} + \psi \right)$$여기서 좌표계의 회전을 나타내는 매개변수는 $x' = x \cos \theta + y \sin \theta$ 및 $y' = -x \sin \theta + y \cos \theta$ 이다.
이 수식에서 $\lambda$는 변조되는 사인파의 파장(Wavelength)으로, 필터가 반응하는 공간 주파수의 역수를 의미한다. $\theta$는 필터의 방향(Orientation)을 결정하며 패드의 배경 줄무늬가 뻗어 있는 각도와 일치시킬 때 가장 강한 응답을 유도할 수 있다. $\sigma$는 가우시안 포락선(Envelope)의 표준 편차로 필터가 이미지를 훑는 유효 영역의 크기를 조절하고, $\gamma$는 공간 가로세로비(Spatial aspect ratio)로서 가우시안 윈도우의 타원형 찌그러짐 정도를 지정한다. 마지막으로 $\psi$는 위상 오프셋(Phase offset)을 의미한다. 이 매개변수들의 정밀한 튜닝을 통해 연구자는 특정한 패드 제조 공정에서 발생하는 고유의 텍스처 주파수 대역만을 표적으로 삼는 맞춤형 필터를 설계할 수 있다.2.2. 대역 차단 메커니즘을 위한 1-Gabor 역산(Inverse) 필터링 기법전통적인 가보어 필터는 대역 통과 필터(Band-pass filter)로 기능한다. 즉, 지정된 주파수 대역과 방향에 일치하는 이미지 내의 텍스처 성분만을 통과시키고 나머지 성분들은 억제하는 역할을 강제한다. 만약 우리가 찾고자 하는 결함 자체가 특정한 질감을 띄고 있다면 대역 통과 필터가 유용하겠지만, 프로브 마크 검사에서의 목표는 그 반대이다. 우리는 규칙적인 배경 패턴을 '차단(Reject)'하고, 불규칙적인 스크래치 형상인 프로브 마크의 특징만을 보존해야 한다. 이를 위해서는 배경 패턴의 주파수를 억제하는 가보어 대역 차단 필터(Gabor Band-reject filter), 흔히 '1-Gabor'라 불리는 구조를 채택해야 한다.1-Gabor 대역 차단 필터는 가보어 대역 통과 필터가 통과시키는 모든 주파수 대역을 반대로 거부(Reject)하고, 대역 통과 필터가 거부하는 모든 영역(즉, 배경 주파수에 해당하지 않는 모든 무작위적이고 돌발적인 고주파/저주파 성분들)을 수용하는 방식으로 설계된다. 공간 도메인(Spatial domain)에서 직접적으로 큰 크기의 커널을 컨볼루션(Convolution) 연산하는 것은 계산 비용이 기하급수적으로 증가하는 문제가 있다. 반면, 주파수 도메인(Frequency domain)에서 필터링을 수행하면 이 과정이 매우 효율적이고 직관적으로 변모한다. 일반적인 주파수 도메인 필터링 연산에서는 픽셀 공간의 원본 이미지를 고속 푸리에 변환(Fast Fourier Transform, FFT) 알고리즘을 사용하여 주파수 영역 스펙트럼으로 변환시킨다.변환된 스펙트럼 중심에는 저주파 성분이, 가장자리에는 고주파 성분이 분포하며, 패드의 규칙적인 배경 줄무늬는 스펙트럼 상에서 특정한 방향으로 뻗어나간 밝은 임펄스(Impulse) 형태의 에너지 집중 영역으로 나타난다. 이때, 앞서 수학적으로 정의된 가보어 대역 통과 커널 역시 푸리에 변환을 통해 주파수 도메인으로 옮겨진다. 주파수 도메인에서 1-Gabor 대역 차단 필터는 전체 주파수 공간의 에너지를 1로 두었을 때, 가보어 대역 통과 필터의 응답 계수를 빼는 방식($1 - |G(u,v)|$)으로 손쉽게 구성될 수 있다. 원본 이미지의 푸리에 스펙트럼에 이 대역 차단 필터를 요소 단위(Element-wise)로 곱셈(Multiplication) 연산하면, 배경 패턴에 해당하는 특정 주파수 에너지만이 0에 가깝게 상쇄된다. 마지막으로 이를 역 고속 푸리에 변환(Inverse FFT, IFFT)을 통해 다시 공간 도메인의 픽셀 이미지로 복원하면 필터링이 완료된다.이러한 정교한 1-Gabor 전처리를 거친 최종 출력 이미지는 육안으로 관찰했을 때 패드의 어두운 구조적 영역이나 특유의 거친 질감이 마치 지우개로 지운 것처럼 마법같이 사라지게 되며, 오로지 외부적인 물리적 충격에 의해 발생한 프로브 마크의 불규칙한 엣지와 스크래치 형상만이 높은 명암 대비로 보존된다. 배경의 강한 텍스처 엣지가 사전에 약화되었기 때문에, 후속 공정인 SAM 2의 이미지 인코더가 엉뚱한 배경 패턴을 독립된 객체나 결함으로 분리하려 시도하는 치명적인 오류를 데이터 입력단에서 원천적으로 차단할 수 있다.필터링 기법 분류알고리즘 작동 원리검사 공정에서의 장점기술적 한계 및 단점적용 분야 및 사례Gabor 대역 통과 필터 (Band-pass)텍스처 특정 방향 및 공간 주파수와 일치하는 성분만 추출특정 결함이 일관된 질감을 가질 때 결함 엣지 강조에 탁월배경 패턴의 주파수와 결함 주파수가 혼재할 경우 분리 불가능직물 표면 질감 분류, 단순 엣지 감지 1-Gabor 대역 차단 필터 (Band-reject)타겟으로 지정된 배경 주파수를 주파수 도메인에서 상쇄 및 차단타겟 패턴을 제외한 모든 정보를 비파괴적으로 보존파장, 방향, 위상 등 필터 파라미터에 대한 정밀한 수학적 튜닝 필요반도체 패드 검사, 규칙적 구조적 노이즈 제거 히스토그램 임계값 이진화 (Thresholding)픽셀 명암 분포 및 클래스 간 분산(예: Otsu)에 기반한 분리연산 복잡도가 극히 낮아 실시간 인라인 검사 처리에 용이조명 변화에 극도로 취약하며, 복잡한 배경 질감 억제력 전무조명 조건이 완벽히 통제된 단순 명암비 검사 형태학적 필터링 (Morphological)구조화 요소(Structuring element)를 이용한 침식/팽창 연산 반복미세한 점 형태의 노이즈(Salt and pepper) 제거에 효과적결함 객체의 형상 자체가 왜곡될 위험이 큼바이너리 마스크 후처리, 형태 기반 노이즈 제거2.3. 특징 융합(Feature Fusion)을 통한 모델 입력 다각화 설계전처리 과정을 성공적으로 마친 1-Gabor 이미지를 단순히 원본을 대체하는 흑백의 단일 채널(Single-channel) 이미지로만 사용하여 모델에 입력하는 것은 오히려 딥러닝 모델이 활용할 수 있는 정보의 다양성을 축소시킬 우려가 있다. 최신 결함 분류 연구에 따르면, 결함 검출의 재현율(Recall)과 정밀도(Precision)를 극대화하기 위해서는 원본 카메라 검사 이미지와 가보어 필터가 적용된 이미지를 독립적인 스트림으로 추출한 후 이를 완전 연결 계층(Fully connected layer)이나 채널 축에서 병합(Concatenation)하는 듀얼 스트림(Dual-stream) 네트워크 추출 방식이 단일 스트림 방식에 비해 압도적인 성능 향상을 보였다.SAM 2의 입력 인터페이스에 이 개념을 접목하기 위해서는 특징 융합 전략이 필수적이다. SAM 2는 기본적으로 자연계의 풍부한 정보를 담은 RGB 3채널 이미지를 기대하도록 사전 학습되어 있다. 따라서, 단순히 1채널 회색조 전처리 이미지를 복제하여 3채널로 만드는 것보다, 3채널 중 한 개 또는 두 개의 채널(예: R, G 채널)에는 원본 패드 이미지를 할당하여 풍부한 원래의 명암 정보를 보존하고, 나머지 채널(예: B 채널)에는 1-Gabor 필터링을 거쳐 구조적 노이즈가 제거되고 엣지만이 강조된 이미지를 프롬프트 가이드 채널로서 매핑하는 방식이 고려될 수 있다. 이러한 다중 모달리티적 입력 설계는 SAM 2의 비전 트랜스포머 인코더가 원본의 문맥적(Contextual) 정보와 필터링된 특징 맵의 형태적(Morphological) 정보를 동시에 학습하고 어텐션 가중치를 분배하게 함으로써, 결과적으로 프로브 마크의 형상에만 모델의 연산력을 집중시키도록 유도하는 강력한 시각적 힌트가 된다.3. Segment Anything Model 2 (SAM 2)의 구조적 메커니즘과 영샷 한계 분석3.1. SAM 2 파운데이션 모델의 아키텍처적 진보성전처리된 이미지가 준비되었다면, 이를 바탕으로 세그멘테이션을 수행할 두뇌 역할의 모델이 필요하다. 메타(Meta)가 공개한 SAM 2는 이미지 분할 패러다임을 바꾼 강력한 파운데이션 모델이다. 이 모델은 기존 SAM 1의 기능을 비디오 영역까지 확장하면서도 단일 이미지 세그멘테이션에서 비교를 불허하는 속도와 정확도를 달성했다. SAM 2의 아키텍처는 크게 세 가지 핵심 컴포넌트로 나뉜다.첫째, 무거운 연산을 담당하는 고해상도 비전 트랜스포머(Vision Transformer, ViT) 기반의 이미지 인코더(Image Encoder)이다. 이 인코더는 입력 이미지로부터 공간적 특징이 응축된 이미지 임베딩(Image Embedding) 텐서를 단 한 번의 순방향 연산(Forward pass)을 통해 추출한다. 둘째, 사용자 또는 에이전트로부터 주어지는 점, 바운딩 박스, 또는 텍스트 입력의 기하학적, 의미론적 정보를 벡터 형태로 변환하는 프롬프트 인코더(Prompt Encoder)이다. 셋째, 이미지 임베딩과 프롬프트 임베딩을 어텐션 메커니즘으로 교차 결합하여 최종적인 분할 마스크(Segmentation Mask)를 놀라운 속도로 실시간으로 생성해내는 가벼운 마스크 디코더(Mask Decoder)이다.특히 SAM 2는 이전 프레임이나 과거 상호작용에서의 객체 특징을 저장해 두는 메모리 뱅크(Memory Bank) 모듈을 새롭게 도입하였다. 이 아키텍처적 추가로 인해 모델은 시간적으로 변화하는 동적 환경이나 순차적인 상호작용 설정에서 이전의 지식을 문맥(Context)으로 활용하여 시공간적 전파 능력을 획득하였다. SAM 2는 총 1,100만 장 이상의 고해상도 이미지 위에 11억 개의 다채로운 마스크가 매핑된 SA-1B 데이터셋과, 5만여 개의 비디오 기반 SA-V 데이터셋을 활용해 사전 학습되었다. 이러한 천문학적인 규모의 학습 데이터 덕분에 SAM 2는 의료, 항공, 산업 검사 등 자신이 훈련 과정에서 단 한 번도 보지 못한 새로운 도메인의 이미지에 대해서도 강력한 영샷 지식 전이(Zero-shot Knowledge Transfer) 능력을 뽐낼 수 있다.3.2. 산업용 결함 검사 환경에서 영샷(Zero-shot) 추론이 직면하는 한계자연계의 일상적인 객체(자동차, 고양이, 가구 등)에 대해서는 SAM 2가 완벽에 가까운 영샷 분할 성능을 입증하지만, 프로브 마크 세그멘테이션을 비롯한 복잡한 산업용 비전 검사 도메인으로 넘어오면 모델의 정확도는 급격히 하락한다. 이는 단순히 모델의 용량 문제가 아니라, 범용성을 지향하여 학습된 모델의 근본적인 인식 구조에서 기인하는 몇 가지 결정적인 한계점 때문이다.시맨틱 클래스 불가지론(Class-agnostic) 특성과 돌출부 편향: SAM 2는 분할한 마스크에 '강아지', '프로브 마크', '노이즈'와 같은 명시적인 의미론적 클래스 라벨(Semantic Class Label)을 할당하지 않는다. 사용자의 명시적인 텍스트나 정밀한 기하학적 프롬프트가 입력되지 않은 영샷 상황에서 모델을 작동시키면, 모델은 화면 내에 존재하는 명암의 변화가 크거나 구조가 뚜렷한 모든 돌출부(Saliency)를 맹목적으로 찾아내어 마스크로 덮어버린다. 반도체 패드처럼 복잡한 텍스처를 가진 배경에서는 이 불가지론적 특성이 치명적인 과분할을 초래한다.모방 객체(Look-alike Objects) 및 미세 결함의 시각적 모호성: 산업용 결함은 자연계 객체와 달리 구조적으로 비정형적이고 시각적으로 모호하며 발생 빈도가 낮다. 미세한 스크래치, 머리카락 두께의 균열, 그리고 낮은 대조도(Low-contrast)를 갖는 개재물들은 정상적인 구조물과 너무나 닮아 있다. 프로브 패드의 그레인이나 연마 스크래치는 프로브 니들의 타격 마크와 구분하기가 극도로 어렵다. 바닐라(Vanilla) SAM 알고리즘은 이렇게 희소하고 식별이 까다로운 미세 결함을 무시하고, 오히려 화면의 넓은 면적을 차지하거나 뚜렷한 엣지를 형성하는 굵직한 패드의 알루미늄 패턴을 주요 객체로 선택해버리는 데이터 불균형적 편향성을 지닌다.정적 파라미터로 인한 오차의 영구적 누적 현상: SAM 2는 대화형(Interactive) 구조를 갖추고 있어 사용자가 잘못된 분할 결과를 보고 이를 수정하기 위해 새로운 포인트 프롬프트를 지속적으로 입력할 수 있다. 모델 아키텍처에 내장된 메모리 뱅크는 이러한 사용자 상호작용의 수정 내역을 조건부 입력이나 최근 문맥으로 임시 저장하여 현재 프레임의 추론에 반영한다. 그러나 모델의 심층 가중치 파라미터(Weight Parameters) 자체는 철저하게 동결(Frozen)되어 있다. 즉, 모델은 사용자 피드백으로부터 본질적인 결함 판단 기준을 내부화(Internalize)하거나 일반화(Generalize)하지 못한다. 결과적으로, 다음 웨이퍼 장에서 완전히 동일한 배경 패턴으로 인한 오류가 발생하더라도 모델은 이를 스스로 교정하지 못하고 끊임없이 인간 작업자의 반복적인 개입을 요구하게 되어, 자동화 검사 장비로서의 실효성을 잃게 된다.4. 인간 지능 모사를 위한 인터랙티브 에이전트 및 강화학습(RL) 모델링앞서 언급한 SAM 2의 한계는 모델이 이미지 공간 상의 픽셀 차이는 계산할 줄 알지만, "현재 검사하고자 하는 공정의 목적은 무엇인가?"라는 고차원적인 맥락적 사고가 결여되어 있기 때문에 발생한다. 숙련된 품질 검사자는 며칠간 훈련을 받고 나면 배경의 복잡한 노이즈 패턴과 실제 불량을 야기하는 프로브 마크를 직관적으로 분리해 낸다. 이들이 활용하는 이러한 고도화된 도메인 지식과 직관을 딥러닝 시스템에 정착시키기 위한 최적의 방법론은, 에러를 내뱉는 정적인 모델 곁에 동적이고 능동적인 강화학습 에이전트(Reinforcement Learning Agent)를 배치하는 것이다. 에이전트는 SAM 2를 하나의 강력한 도구(Tool)로 삼아 지속적인 시도-오류(Trial-and-error) 루프를 돌며 최적의 세그멘테이션 정책을 학습한다.4.1. 상호작용적 세그멘테이션을 위한 마르코프 결정 과정(MDP) 공식화지도 학습(Supervised Learning)이 모델에게 "이 이미지의 정답은 이 마스크다"라고 한 번에 주입식으로 가르치는 것이라면, 강화학습은 "마스크를 수정하기 위해 이 좌표에 점을 찍었더니 결과가 좋아졌네? 이 행동을 강화하자"라는 점진적인 의사결정 과정을 학습하는 것이다. SAM 2를 통제하기 위한 에이전트를 설계하려면 이 인터랙티브 세그멘테이션 과정을 에피소드 기반의 다단계 자율 의사결정 프레임워크인 마르코프 결정 과정(Markov Decision Process, MDP)으로 정교하게 수학화하여 접근해야 한다.상태 공간 (State Space, $S_t$): 강화학습 에이전트가 매 결정 스텝(Step $t$)마다 관찰하고 판단의 근거로 삼는 환경 데이터이다. 이는 매우 거대한 텐서 공간이다. 1-Gabor 필터링 등 전처리 과정을 완벽히 마친 프로브 패드의 원본 영상, 현재까지 에이전트가 찍었던 점(Point) 프롬프트 좌표들의 가우시안 맵(Gaussian Map) 표현, 그리고 방금 직전 스텝에서 SAM 2 모델이 추론하여 반환한 바이너리 분할 마스크(Binary Segmentation Mask) 결과물이 채널 단위로 쌓여서 하나의 거대한 상태 벡터를 구성하게 된다.행동 공간 (Action Space, $A_t$): 에이전트가 현재의 상태 정보를 인지한 후 환경에 대해 취할 수 있는 조작이다. 상호작용형 비전 환경에서 에이전트의 행동은 곧 "SAM 2 모델에게 입력으로 제공할 가장 유익한(Informative) 프롬프트의 매개변수를 결정하는 것"이다. 이는 구체적으로 다음 번에 추가할 긍정적 점(객체 내부 좌표) 또는 부정적 점(배경으로 제외할 좌표)의 2차원 $(X, Y)$ 실수 좌표를 연속 공간에서 예측하거나, 새로운 바운딩 박스를 지정하는 형태가 된다.보상 함수 (Reward Function, $R_t$): 에이전트가 어떤 좌표에 점을 찍는 행동을 취한 결과로, SAM 2가 새로운 분할 마스크를 출력했을 때 환경으로부터 주어지는 평가 피드백 신호이다. 시스템 초기 구축 단계에서는 전문가가 힘들게 일일이 그려놓은 소량의 정답 마스크(Ground Truth, GT) 데이터를 활용한다. 에이전트의 새로운 마스크와 GT 간의 Intersection over Union (IoU) 값을 계산하고, 이전 스텝에서의 IoU 대비 얼마나 품질이 향상되었는가에 대한 상대적 증가분($\Delta$IoU)을 핵심 보상 스칼라(Scalar) 값으로 산출하여 에이전트에게 부여한다.4.2. 휴먼 피드백 기반 강화학습(RLHF) 파이프라인의 적용전문가 주석이 달린 GT 데이터에 의존하여 $\Delta$IoU로 보상을 주는 방식은 고정된 데이터셋 내에서는 잘 동작하지만, 앞서 언급했듯 산업 검사에서 불량 형상은 "정답"을 수학적으로 엄밀히 정의하기가 모호한 경우가 부지기수다. 전문가들조차도 마스크 픽셀의 미세한 경계면을 놓고 의견이 엇갈리며, 작업자의 시각적 "느낌(Vibe)"이나 수십 년간 축적된 노하우가 정답 기준에 개입된다. 이러한 상황을 돌파하기 위해 최근 대형 언어 모델(LLM) 정렬(Alignment)에 핵심적으로 사용되는 RLHF(Reinforcement Learning from Human Feedback) 방법론을 이미지 세그멘테이션 정책 최적화 영역으로 이식하려는 시도가 각광받고 있다.비전 세그멘테이션 에이전트를 위한 RLHF 파이프라인은 다음과 같은 정밀한 절차로 구성된다.다중 마스크 수집 및 인간 선호도 데이터 구축: 동일한 프로브 패드 전처리 이미지 한 장에 대하여, 무작위 정책을 따르는 기초 에이전트가 각기 다른 점 위치를 입력하여 서로 다른 형태의 SAM 2 세그멘테이션 결과(마스크 A, 마스크 B)를 복수로 생성해 낸다.보상 모델(Reward Model)의 학습: 숙련된 실제 검사 작업자(인간)가 이 두 마스크를 비교 평가한다. "어느 마스크가 프로브 마크의 핵심 결함 부위를 더 온전히 감싸고 있으며, 동시에 배경의 불필요한 줄무늬 텍스처를 더 철저하게 배제하였는가?"라는 복합적이고 정성적인 기준을 바탕으로 우열 순위(Preference Ranking)를 매긴다. 인간이 내린 이 순위 데이터를 지도 학습의 손실 함수(Loss function)로 사용하여, 이미지와 마스크를 입력으로 받았을 때 그 품질 스코어를 연속적인 스칼라 값으로 예측해내는 독립된 딥러닝 보상 네트워크(Reward Network)를 훈련시킨다. 이 보상 모델은 인간 검사자의 무의식적인 시각적 판별 기준을 고스란히 수학적 함수로 증류(Distillation)한 결정체이다.정책 최적화(Policy Optimization) 알고리즘 적용: 인간 검사자가 잠들거나 퇴근한 이후에도 학습은 멈추지 않는다. 에이전트 정책 네트워크는 새로운 패드 이미지를 보고 특정 픽셀 좌표에 행동(점 프롬프트 생성)을 취하고 SAM 2를 구동시킨다. 사전에 철저히 학습된 보상 모델이 이 결과를 평가하여 스칼라 보상을 산출해 돌려준다. 에이전트는 이 피드백을 기반으로 근접 정책 최적화(Proximal Policy Optimization, PPO) 혹은 그룹 상대 정책 최적화(Group Relative Policy Optimization, GRPO) 알고리즘을 사용해, 자신에게 주어지는 장기적 누적 보상(Expected return)의 기댓값을 극대화하는 방향으로 신경망 가중치를 지속적이고 안정적으로 업데이트하게 된다.4.3. 파운데이션 모델 제어를 위한 선도적 강화학습 연구 사례 심층 비교순수한 비전 모델에 강화학습을 도입하여 상호작용성을 부여하는 아키텍처적 시도는 최근 최상위 컴퓨터 비전 학회 논문들을 통해 그 성능의 우월성이 강력히 입증되고 초점을 맞추고 있다. 이러한 연구 결과들은 본 보고서에서 제안하는 PMI 자동화 모델 구조 설계의 강력한 레퍼런스가 된다.AlignSAM (Aligning SAM to Open Context via RL): CVPR 2024에 발표된 AlignSAM은 변화무쌍한 열린 세계(Open-world)의 미지 객체를 분할하기 위해 바닐라 SAM 구조 위에 독립된 강화학습 에이전트(정책 네트워크)를 접목한 파이오니어 연구이다. 이 에이전트는 주어진 영상의 시각적 특징을 심층 분석하여 SAM에게 제공할 "가장 유익한 점(Informative point)"의 최적 좌표를 스스로 학습해 찾아낸다. 더불어 이들은 시맨틱 재보정 모듈(Semantic Recalibration Module)을 추가 도입하여, 에이전트가 찍은 점이 배제해야 할 '배경(네거티브 프롬프트)'인지 포함시켜야 할 '객체(포지티브 프롬프트)'인지를 이진 분류(Binary classification)하도록 설계함으로써 파운데이션 모델이 명시적인 의미(Semantic) 태스크를 처리할 수 있는 역량을 극대화했다. PMI에 적용 시, 에이전트는 알루미늄 그레인 패턴에는 지속적으로 네거티브 점을 투하하고, 깊게 패인 마크의 계곡부에는 포지티브 점을 투하하는 일련의 전략을 완전히 스스로 터득할 수 있게 된다.MedSAM-Agent (Multi-turn Agentic RL for Medical Imaging): 의료 영상 분석 분야의 병변 조직은 반도체의 미세 결함과 유사하게 시각적 경계가 극도로 모호하여 정교한 조작이 필수적이다. 기존 연구들이 단 한 번(Single-turn)의 상호작용에 그쳤던 반면, MedSAM-Agent 프레임워크는 상호작용적 분할(Interactive Segmentation) 과정을 전문가가 직접 모니터링하듯 다중 턴(Multi-turn) 에이전틱 자율 의사결정 과정으로 재구성하였다. 특히 전문가가 수정하는 과정을 그대로 녹화한 궤적(Expert-curated trajectory)을 초기 프롬프트 전략으로 모델에 내재화시켰으며, 임상적 충실도를 반영한 과정 지향 보상(Process reward)을 정교하게 설계하여 불필요한 반복 조작(Redundant actions)을 크게 줄이면서 최고 수준(SOTA)의 성능을 달성하였다.LENS (Learning to Segment Anything with Unified Reinforced Reasoning): 가장 급진적인 연구 중 하나인 LENS 프레임워크는 단순히 점을 찍는 수준을 넘어서, Qwen-VL과 같은 대규모 시각-언어 모델(MLLM)의 텍스트 추론 능력과 픽셀 분할 모델을 하나로 융합하였다. 이들은 생각의 사슬(Chain-of-Thought, CoT) 추론 과정을 도입하여 분할해야 할 대상을 언어적으로 규명하고, 통일된 강화 추론(Unified Reinforced Reasoning) 체계를 구축했다. 문장 수준의 논리적 사고, 객체의 물리적 바운딩 박스 지역화, 그리고 픽셀 단위의 분할 마스크 품질 등 이질적인 요소들을 GRPO 최적화라는 단일 강화학습 목적 함수(Objective function) 안에 결합함으로써, 모델이 모호한 지시에도 논리적으로 객체를 찾아내는 일반화 능력을 비약적으로 끌어올렸다.방법론적 접근 방식아키텍처 및 학습 메커니즘의 특징프로브 마크 검사 도입 시의 장점산업 환경 적용 시 기술적 장애 요인 및 고려사항순수 SAM 2 (Zero-shot)수십억 장 데이터로 사전 학습된 강력한 ViT 기반 인코더와 프롬프트 기반 가벼운 디코더 구조별도의 고비용 마스크 라벨링 작업 불필요, 신규 모델 배포 및 빠른 개념 증명(PoC) 구축 가능명시적 프롬프트 부재 시, 거친 패드 텍스처를 마크로 인식하는 돌출부 편향 회피 불가능 완전 지도 미세 조정 (Supervised Fine-tuning)수집된 도메인 이미지와 GT 마스크를 이용해 U-Net 등을 훈련하거나 SAM 디코더 가중치 업데이트훈련 데이터에 존재하는 환경 조명 및 한정된 결함 양상에 대해서는 가장 높은 픽셀 정확도 도달 가능수천 장의 초정밀 픽셀 단위 마스크를 인간이 직접 칠해야 하는 막대한 인건비 소모 및 과적합 위험성 RL Agent + SAM 2 결합PPO 알고리즘 기반 독립 에이전트가 상태를 평가하고 가장 유익한 프롬프트 좌표 지속 예측 (AlignSAM 등) 동적인 시도-오류를 통한 강력한 자가 개선, 인간 검사자의 단계별 상호작용 추론 패턴 그대로 모방수만 픽셀 차원에 달하는 방대한 상태(State) 공간 처리 및 수렴을 위한 보상 함수 설계 난이도 극상 RLHF (Human Feedback)인간의 정성적 선호도와 직관(Vibe)을 심층 신경망을 통해 정량적 스코어로 매핑하는 보상 모델 운용 '정답 마스크'의 정의 자체가 모호하고 전문가 의견이 엇갈리는 복잡한 텍스처-이상치 간 경계 분리에 탁월막대한 양의 모델 출력 마스크를 전문가가 비교 평가하여 초기 피드백 랭킹 데이터셋을 구축해야 하는 병목 현상 발생 결론적으로 강화학습을 결합한 에이전트의 성공 여부는 에이전트의 목표를 조향하는 보상 함수(Reward Function) 설계에 전적으로 달려있다. 프로브 마크 검사 태스크를 위한 보상을 설계할 때는 단순히 "생성된 마스크가 프로브 마크 픽셀을 얼마나 덮고 있는가"를 평가하는 양성 보상(Positive Reward) 뿐만 아니라, "마스크의 경계선이 배경 패턴의 특정 고주파 요철 영역을 얼마나 정교하게 회피하고 있는가"를 가늠하여 오분할 시 페널티를 부여하는 부정적 보상(Negative Reward) 항목을 명시적으로 수식에 포함시켜 에이전트가 엄격한 경계 준수 정책을 학습하도록 강제해야 한다.5. 학습 역량 강화를 위한 컴퓨터 비전 및 강화학습 분야 권장 문헌본 연구에서 제시하는 거대한 파이프라인(고전적 주파수 이미지 전처리, 최신 파운데이션 모델 활용, 최첨단 강화학습 최적화)을 실제 현장 시스템으로 통합하고 엔지니어링 역량을 함양하기 위해서는 기초 이론부터 최신 딥러닝 아키텍처에 이르기까지 깊이 있는 이론적 문헌 학습이 병행되어야 한다. 다음 서적들은 본 연구의 방법론을 완벽히 이해하는 데 필수적인 지식을 망라하여 제공한다.Computer Vision: Algorithms and Applications (Richard Szeliski 저) 
이 책은 컴퓨터 비전 분야 연구자와 엔지니어들의 영원한 바이블로 통한다. 딥러닝 기반 패러다임이 등장하기 이전의 근본적인 영상 처리 수학부터 최신 머신러닝 기법까지를 가장 포괄적이고 학술적으로 다룬다. 본 보고서의 2장에서 논의된 가보어 필터 설계, 푸리에 변환(FFT)의 주파수 분석, 모폴로지(Morphology) 영상 처리 등의 기초적이고 고전적인 메커니즘을 심도 있게 수학적으로 이해하고자 할 때 가장 독보적인 통찰을 제공한다. 무료 PDF 형식으로도 제공되어 접근성이 뛰어나다.Deep Learning for Vision Systems (Mohamed Elgendy 저) 
강력한 컴퓨터 비전 모델을 구축하고 실제 산업 환경에 파이프라인으로 배포(Deployment)하는 실무적 구현에 초점을 맞춘 최고의 실전 안내서이다. 이미지 분류, 객체 탐지, 그리고 시맨틱 분할(Semantic Segmentation) 등 컴퓨터 비전의 난제들을 해결하기 위해 TensorFlow 및 PyTorch와 같은 딥러닝 프레임워크를 어떻게 활용해야 하는지 상세한 예제와 함께 서술한다. CNN 및 전이 학습의 구조를 이해하여 SAM 2와 같은 모델의 내부 구조를 분해하고 파악하는 데 필수적인 도서이다.Foundations of Computer Vision (Antonio Torralba, Phillip Isola, William Freeman 공저, MIT Press) 
가장 최근에 출판된 문헌 중 하나로 기계 학습, 확률론적 딥러닝, 그리고 고전 이미지 처리의 교차점에서 컴퓨터 비전의 기초를 현대적인 시각으로 재조명한다. 특히 이미지 공간(Image Space)의 분포적 특성과 생성형(Generative) 모델, 트랜스포머 기반의 파운데이션 모델(Foundation Models)이 픽셀 데이터를 어떻게 저차원 임베딩 공간(Embedding Space)으로 매핑하여 특징을 추출하는지를 다루고 있어, 본 연구의 SAM 2의 작동 원리에 대한 본질적인 직관을 얻기에 매우 훌륭하다.Deep Learning for Computer Vision: A Practitioner's Guide 
전통적인 수제 특징 기반(Handcrafted feature) 파이프라인에서 시작해, 최신 종단간(End-to-end) 딥러닝 학습으로 진화하는 산업용 컴퓨터 비전(Industrial Computer Vision) 트렌드를 집중적으로 파헤친 실무 중심의 가이드이다. ResNet의 잔차 연결 메커니즘부터 강력한 비전 트랜스포머(ViT), 계층적 트랜스포머(Hierarchical Transformers), 그리고 영상(Video) 처리에 이르는 최첨단 기초 모델들이 실제 생산 및 결함 품질 통제 공정에 적용되는 상세한 아키텍처 사례(Case Studies)와 최적화 기법을 제공한다.6. 주파수 전처리 및 강화학습 상호작용 환경 구축을 위한 파이썬(Python) 기반 프로토타입 구현본 보고서 전반에 걸쳐 논의된 복잡한 이론적 아키텍처(1-Gabor 주파수 대역 차단 필터, SAM 2 추론, 상호작용을 위한 PPO 에이전트 환경)를 단일 파이프라인으로 구체화하기 위해 Python 언어를 기반으로 한 실질적인 코드 프로토타입을 제시한다. 제시된 코드는 OpenAI의 표준 환경 규격인 Gymnasium을 이용하여 강화학습 에이전트의 관찰(Observation), 행동(Action), 보상(Reward) 체계를 완벽하게 캡슐화한 커스텀 환경 구성을 포함한다.6.1. 하드웨어 환경 및 라이브러리 종속성 설정본 구현은 높은 연산량을 요구하므로 최신 GPU 자원(CUDA 지원)이 권장되며, 전처리 연산을 위한 OpenCV와 행렬 연산용 NumPy, 그리고 RL 에이전트 학습을 위한 Stable Baselines3 등 다수의 라이브러리 간 버전 호환성 설정이 매우 중요하다.Bash# 독립적인 가상 환경 구축을 통한 종속성 충돌 방지
conda create -n probe_pmi_rl python=3.11 -y
conda activate probe_pmi_rl

# 영상 처리 및 강화학습 핵심 패키지 설치
pip install opencv-python numpy matplotlib
pip install gymnasium stable-baselines3[extra] torch torchvision torchaudio

# 메타(Meta)의 원본 SAM 2 저장소 복제 및 로컬 빌드 설치 
git clone https://github.com/facebookresearch/sam2.git
cd sam2
pip install -e.
6.2. 1-Gabor 전처리 모듈 및 커스텀 RL 환경(Gymnasium) 결합 소스 코드이 파이썬 스크립트는 패드의 알루미늄 줄무늬를 제거하기 위해 푸리에 공간에서 대역 차단 연산을 수행하는 1-Gabor 전처리 함수와, 에이전트가 점 좌표를 찍어가며 IoU 향상치를 보상으로 받아 지속적으로 마스크를 갱신하는 MDP 기반 RL 환경을 정밀하게 묘사하고 있다.Pythonimport cv2
import numpy as np
import torch
import gymnasium as gym
from gymnasium import spaces
from sam2.build_sam import build_sam2
from sam2.sam2_image_predictor import SAM2ImagePredictor

# ==============================================================================
# 1. 고주파/배경 패턴 억제를 위한 1-Gabor 대역 차단 필터 모듈 (주파수 도메인 연산)
# ==============================================================================
def apply_inverse_gabor_band_reject(image_gray, ksize=31, sigma=4.0, theta=np.pi/4, lam=10.0, gamma=0.5):
    """
    고전적인 OpenCV 가보어 커널을 생성한 뒤, 고속 푸리에 변환(FFT)을 사용하여 
    특정 방향과 주파수를 띠는 패드의 배경 텍스처(줄무늬 등)를 억제하는 1-Gabor 필터.[10, 19, 23, 24]
    이 전처리를 거치면 엣지는 둔화되고 프로브 마크의 불규칙한 타격흔만 강하게 대비됨.
    """
    # 1. 지정된 공간 주파수(lam)와 방향(theta)에 민감하게 반응하는 대역 통과 커널 생성
    gabor_kernel = cv2.getGaborKernel((ksize, ksize), sigma, theta, lam, gamma, 0, ktype=cv2.CV_32F)
    
    # 2. 원본 이미지를 FFT 연산하여 주파수 도메인 스펙트럼으로 매핑 후, 중심부 이동
    f_transform = np.fft.fft2(image_gray)
    f_shift = np.fft.fftshift(f_transform)
    
    # 3. 공간 도메인의 커널을 원본 이미지와 동일한 크기로 패딩(Padding)하여 주파수 응답 확보
    h, w = image_gray.shape
    padded_kernel = np.zeros((h, w), dtype=np.float32)
    cy, cx = h // 2, w // 2
    kh, kw = ksize, ksize
    padded_kernel[cy-kh//2 : cy+kh//2+1, cx-kw//2 : cx+kw//2+1] = gabor_kernel
    
    # 4. 패딩된 가보어 커널의 주파수 도메인 진폭 스펙트럼 도출 및 최대치 1로 정규화
    kernel_fft = np.fft.fftshift(np.fft.fft2(padded_kernel))
    kernel_magnitude = np.abs(kernel_fft)
    # 0으로 나누기 방지를 위한 안전 장치 삽입
    max_val = np.max(kernel_magnitude)
    if max_val > 0:
        kernel_magnitude = kernel_magnitude / max_val
    
    # 5. 핵심: 1-Gabor 메커니즘. 통과 대역을 반대로 빼서 차단 대역(Band-reject) 마스크 생성
    band_reject_mask = 1.0 - kernel_magnitude
    
    # 6. 원본 이미지 주파수 스펙트럼에 차단 마스크를 요소 단위(Element-wise)로 곱셈 적용
    filtered_shift = f_shift * band_reject_mask
    
    # 7. 억제된 스펙트럼을 공간 도메인의 이미지로 복원하기 위한 역 푸리에 변환(IFFT)
    filtered_ishift = np.fft.ifftshift(filtered_shift)
    image_filtered = np.abs(np.fft.ifft2(filtered_ishift))
    
    # 8. 소실된 픽셀 다이나믹 레인지를 복구하기 위해 Min-Max 방식 기반 0~255 정규화 처리
    image_normalized = cv2.normalize(image_filtered, None, 0, 255, cv2.NORM_MINMAX, dtype=cv2.CV_8U)
    
    # SAM 2 모델은 기본적으로 자연 환경의 3채널(RGB) 이미지를 기대하도록 학습되었으므로 확장 변환 수행
    return cv2.cvtColor(image_normalized, cv2.COLOR_GRAY2RGB)

# ==============================================================================
# 2. SAM2 백본 기반 상호작용적 마르코프 결정 과정(MDP) 강화학습 환경 클래스 구축
# ==============================================================================
class ProbeMarkInteractiveEnv(gym.Env):
    """
    OpenAI Gymnasium 규격을 준수하는 커스텀 RL 환경.[14, 33]
    이 환경 내부에 SAM 2가 임베딩되어 있으며, 에이전트는 이미지와 기존 마스크 궤적을 
    관찰(Observation)하고 새로운 프롬프트 좌표를 산출(Action)하여 최적화를 수행한다.
    """
    def __init__(self, image_path, mask_gt_path, sam_checkpoint, model_cfg):
        super(ProbeMarkInteractiveEnv, self).__init__()
        
        # 1. 디스크로부터 패드 검사 이미지 및 전문가 주석 정답(Ground Truth) 로드
        image_bgr = cv2.imread(image_path)
        self.image_gray = cv2.cvtColor(image_bgr, cv2.COLOR_BGR2GRAY)
        self.gt_mask = cv2.imread(mask_gt_path, cv2.IMREAD_GRAYSCALE)
        
        # GT를 엄격한 0과 255의 이진 형태(Binary mask)로 변환
        _, self.gt_mask = cv2.threshold(self.gt_mask, 127, 255, cv2.THRESH_BINARY)
        self.height, self.width = self.image_gray.shape
        
        # 2. 모델 인식을 방해하는 배경 패턴 억제를 위해 1-Gabor 전처리 실행
        self.processed_image = apply_inverse_gabor_band_reject(self.image_gray)
        
        # 3. CUDA 가속을 지원하는 디바이스 탐색 및 SAM 2 모델 추론기 메모리 적재 [6, 8]
        device = "cuda" if torch.cuda.is_available() else "cpu"
        sam_model = build_sam2(model_cfg, sam_checkpoint, device=device)
        self.predictor = SAM2ImagePredictor(sam_model)
        
        # 프롬프트 입력에 따른 빠른 마스크 디코딩을 위해 이미지의 고해상도 ViT 임베딩 사전 연산
        self.predictor.set_image(self.processed_image)
        
        # 4. 행동 공간(Action Space) 정의: 정규화된 2차원 실수 좌표 공간
        # 에이전트는 0.0과 1.0 사이의 연속적인 값을 출력하며, 환경에서 이를 픽셀 스케일로 변환함 [33, 34]
        self.action_space = spaces.Box(low=0.0, high=1.0, shape=(2,), dtype=np.float32)
        
        # 5. 상태 공간(Observation/State Space) 정의: H x W x 4 차원의 복합 텐서
        # R, G, B 원본 영상(3채널) + 현재까지 완성된 SAM 2 마스크 진행 상황(1채널) 병합
        self.observation_space = spaces.Box(low=0, high=255, shape=(self.height, self.width, 4), dtype=np.uint8)
        
        # 환경 추적을 위한 내부 제어 변수 초기화
        self.current_mask = np.zeros((self.height, self.width), dtype=np.uint8)
        self.steps = 0
        self.max_steps = 15 # 상호작용 피로도 및 연산 낭비를 막기 위한 한 에피소드 최대 턴 수 제한 [31, 33]

    def reset(self, seed=None, options=None):
        """
        강화학습 에피소드 시작 시 환경을 완전히 초기 상태로 되돌림.
        """
        super().reset(seed=seed)
        self.steps = 0
        self.current_mask = np.zeros((self.height, self.width), dtype=np.uint8)
        
        # 3채널 이미지와 1채널 마스크 텐서를 깊이(depth) 방향으로 병합하여 관찰 상태 생성
        state = np.dstack((self.processed_image, self.current_mask))
        return state, {}

    def step(self, action):
        """
        에이전트가 정책에 따라 예측한 좌표 행동(action)을 전달받아 SAM 2를 작동시키고,
        새로운 상태(State)와 성능 변화율 기반 보상(Reward)을 반환하는 코어 루프.[14, 33]
        """
        # [0.0, 1.0] 범위인 연속 좌표 값을 이미지 차원에 맞는 실제 픽셀 인덱스로 정수 맵핑
        x_coord = int(np.clip(action, 0, 1) * (self.width - 1))
        y_coord = int(np.clip(action, 0, 1) * (self.height - 1))
        
        # SAM 2 인터페이스 규칙에 맞게 프롬프트 데이터 구조체화
        # 1로 세팅된 라벨은 모델에게 이 픽셀 주변이 전경(마크 객체)임을 알려주는 포지티브 점을 의미함
        input_point = np.array([[x_coord, y_coord]])
        input_label = np.array() 
        
        # SAM 2 모델의 마스크 디코더 활성화 (사전 계산된 이미지 임베딩 사용으로 초고속 연산 가능)
        masks, scores, logits = self.predictor.predict(
            point_coords=input_point,
            point_labels=input_label,
            multimask_output=False, # 여러 개의 모호한 결과 중 가장 확률이 높은 단일 마스크 반환 강제
        )
        
        # Boolean 배열로 반환된 마스크를 시각화 및 평가를 위한 8비트 정수 형태로 변환
        predicted_mask = (masks * 255).astype(np.uint8)
        
        # ---------------------------------------------------------
        # 정밀한 평가 및 보상 함수(Reward Function) 연산 로직 
        # ---------------------------------------------------------
        # 새롭게 생성된 마스크와 정답(GT) 마스크 간의 교집합(Intersection) 및 합집합(Union) 픽셀 수 산출
        intersection = np.logical_and(predicted_mask, self.gt_mask).sum()
        union = np.logical_or(predicted_mask, self.gt_mask).sum()
        current_iou = intersection / union if union > 0 else 0.0
        
        # 이전 스텝에서 생성했던 낡은 마스크의 성능 지표(IoU) 산출
        prev_intersection = np.logical_and(self.current_mask, self.gt_mask).sum()
        prev_union = np.logical_or(self.current_mask, self.gt_mask).sum()
        prev_iou = prev_intersection / prev_union if prev_union > 0 else 0.0
        
        # 핵심 설계: 절대적인 성능 수치가 아닌 이전 턴 대비 성능 향상도(Delta IoU)를 보상으로 지급. 
        # 이를 통해 에이전트는 무의미한 지점을 반복 클릭하는 행위를 회피하고 마스크 경계를 확장/정제하는 좌표만을 선택함 [35]
        # 모델 학습 가속을 위해 상수 스케일링(x100) 적용
        reward = (current_iou - prev_iou) * 100.0 
        
        # 상태 변수 갱신 (마스크 덮어쓰기) 및 카운터 증가
        self.current_mask = predicted_mask
        self.steps += 1
        
        # 종료 조건(Termination) 판단 로직
        # 1) 목표 수준의 완벽한 분할 성공(IoU 0.95 이상) 또는 2) 허용된 최대 교정 횟수 도달 시 에피소드 종료
        terminated = bool(current_iou >= 0.95 or self.steps >= self.max_steps)
        truncated = False
        
        # 다음 스텝을 위해 새로운 관찰 텐서 조립
        next_state = np.dstack((self.processed_image, self.current_mask))
        info = {"iou": current_iou, "reward": reward}
        
        return next_state, reward, terminated, truncated, info

# ==============================================================================
# 3. PPO 최적화 알고리즘 기반 실제 에이전트 훈련 실행 예시 블록
# ==============================================================================
if __name__ == "__main__":
    from stable_baselines3 import PPO
    
    # 이 스크립트를 실행하기 전 환경 변수와 경로가 실제 디스크 상황과 일치하는지 확인해야 함.
    # SAM 2 모델의 설정 YAML 및 파라미터 체크포인트 파일 다운로드 선행 필수
    IMAGE_PATH = "sample_pad_surface.jpg"
    MASK_GT_PATH = "sample_pad_mask_groundtruth.png"
    SAM_CKPT = "sam2_hiera_large.pt"
    SAM_CFG = "sam2_hiera_l.yaml"
    
    try:
        # 1. 사용자가 정의한 물리적 전처리 + 가상 SAM2 결합 환경 인스턴스화
        env = ProbeMarkInteractiveEnv(IMAGE_PATH, MASK_GT_PATH, SAM_CKPT, SAM_CFG)
        
        # 2. 이미지 텐서 상태를 다루기 위해 합성곱 신경망(CNN) 특징 추출기를 포함한 PPO 모델 초기화
        # verbose=1은 콘솔에 에이전트의 보상 수렴 과정과 로스 값 로그를 실시간 출력하도록 지시함
        rl_agent = PPO("CnnPolicy", env, verbose=1, tensorboard_log="./ppo_probe_tensorboard/")
        
        # 3. 총 5만 번의 환경 상호작용(프롬프트 입력 및 결과 관찰)을 통한 정책 최적화 훈련 돌입
        print("--- 강화학습 기반 SAM 2 통제 에이전트 훈련 개시 ---")
        rl_agent.learn(total_timesteps=50000)
        
        # 4. 수렴된 에이전트 모델의 신경망 가중치 디스크 저장
        rl_agent.save("ppo_probemark_sam2_policy_v1")
        print("--- 에이전트 모델 저장 성공 ---")
        
    except FileNotFoundError as e:
        print(f"오류: 필요 자원(이미지나 SAM 체크포인트 등)의 경로를 찾을 수 없습니다. ({e})")
위의 Python 스크립트는 학술적 개념으로만 머물던 주파수 도메인 대역 차단 원리와, 초대규모 비전 파운데이션 모델의 제어, 그리고 행동 기반의 PPO 최적화 알고 정교한 알고리즘을 단일 아키텍처 소프트웨어 파이프라인으로 관통하여 구현한 강력한 기술적 시연 모델이다.7. 종합 합성 및 고찰반도체 수율 확보의 핵심인 프로브 카드 패드의 접촉 마크 세그멘테이션은 일반적인 자연 사진과는 본질적으로 다른 복잡성을 지니고 있다. 알루미늄이나 구리 패드가 연마 및 압연 공정에서 얻게 되는 고유의 거친 텍스처와 미세한 직선 형태의 배경 줄무늬는 광학적으로 프로브 핀이 남기는 날카로운 스크래치와 그 주파수 대역이나 명암 대비 면에서 지독하게 닮아 있다. 이러한 환경적 악조건은 아무리 수십억 장의 데이터로 사전 학습된 메타의 최첨단 SAM 2 파운데이션 모델이라 할지라도 단독으로 투입된 영샷(Zero-shot) 추론 상황에서는 맹목적으로 배경을 객체로 오인하는 파괴적인 오분할(Over-segmentation)과 지식 일반화의 실패를 초래하게 만든다.본 보고서에서의 다각적인 심층 분석 결과, 이 난제는 크게 두 가지 이질적인 기술적 스택의 결합을 통해서만 완벽히 타개될 수 있음이 증명되었다. 첫째, 딥러닝 모델로 데이터가 입력되기 전단계에서 광학적 물리 현상에 근거한 결정론적인 주파수 필터링(1-Gabor 대역 차단 필터) 연산을 선제적으로 수행하여야 한다. 이는 푸리에 공간에서의 정교한 역산(Inverse filtering)을 통해 프로브 마크 형태에 손상을 주지 않으면서 모델의 어텐션을 교란하는 배경의 규칙적인 주파수 성분만을 파괴하여 모델의 구조적 맹점(Blind spot)을 보완하는 역할을 수행한다.둘째, 세그멘테이션의 인식 구조 자체를 한 단계 격상시키기 위해, 수십 년 경력을 지닌 인간 검사관의 "복잡한 배경 속에서 결함을 추론하고 분리해내는 역량"을 시스템 단위로 모사해야 한다. 단순히 입력과 출력을 매핑하는 구시대적인 지도 학습 방법론을 탈피하고, 다단계 의사결정 체계(MDP)를 기반으로 PPO 등의 에이전트가 끊임없는 상호작용적 피드백을 주고받는 강화학습(RL) 기반 아키텍처를 전격 도입해야 한다. 여기에 인간 작업자가 분할 마스크의 우열을 가려 만들어낸 선호도 점수 데이터를 기반으로 보상 모델(Reward Model)을 구축하는 RLHF(인간 피드백 기반 강화학습)를 접목한다면, SAM 2의 길들여지지 않은 통제 불능의 범용 분할 능력은 철저히 통제되고 산업 친화적이며 목표 지향적인 추론 능력으로 진화하게 된다.이러한 전처리 필터 기술, 파운데이션 모델, 그리고 고차원적인 에이전틱(Agentic) 피드백 학습 시스템을 통합하는 다중 모달리티 결합 구조는 단지 프로브 마크의 정확한 식별에만 그치지 않는다. 이는 향후 모델 구조의 큰 변형 없이도 메모리 반도체, 시스템 LSI를 막론하고 칩 다이(Die) 전반에 걸쳐 존재하는 무수한 이물질, 파편 개재물, 머리카락 형태의 초미세 스크래치 등 정의하기 모호한 다종의 초정밀 표면 결함들을 능동적으로 추적, 식별, 구획해내는 차세대 무인 완전 자율 비전 검사(Autonomous Vision Inspection) 에이전트를 실현하는 거대한 시스템적 청사진이자 도약의 교두보가 될 것이다.